# Gradio currently imports pydub, and Python 3.13 removed the stdlib audioop module.
# We force Python 3.11 in README.md, but keep audioop-lts as a safety fallback if HF still builds with 3.13+.
audioop-lts; python_version >= "3.13"

gradio>=5.0.0
transformers>=4.52.0
accelerate>=1.7.0
peft>=0.15.0
torch>=2.6.0
sentencepiece>=0.2.0
protobuf>=5.0.0
